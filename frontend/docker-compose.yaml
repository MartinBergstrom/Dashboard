version: '3.8'
services:

  react_vite_docker:
    image: node:alpine
    container_name: react_vite_docker
    entrypoint: /bin/sh
    ports:
      - 3000:3000
    working_dir: /srv/app
    volumes:
      - type: bind
        source: ./
        target: /srv/app
    tty: true

